apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-bot
  namespace: telegram-bots
  labels:
    app.kubernetes.io/name: admin-bot
    app.kubernetes.io/component: bot-instance
    app.kubernetes.io/part-of: telegram-bots
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: admin-bot
  template:
    metadata:
      labels:
        app: admin-bot
        app.kubernetes.io/name: admin-bot
        app.kubernetes.io/component: bot-instance
    spec:
      containers:
        - name: admin-bot
          image: telegram-bots-platform:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "platform_core.cli", "start", "admin_bot"]
          envFrom:
            - configMapRef:
                name: telegram-bots-config
            - secretRef:
                name: telegram-bots-secret
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 384Mi
